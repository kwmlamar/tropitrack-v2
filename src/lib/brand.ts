/**
 * What this thing is called. One place, so the next rename is one line.
 *
 * There have been three names for one product: "TropiTrack" in the repo and the
 * footer, "Bedrock" in the tab and the sidebar, and briefly "Bedrock AI" for the
 * assistant. Settled 2026-09-06:
 *
 *   - The app is **Bedrock**. It is the record — the place the facts live.
 *   - The assistant is just **the Assistant**. It lives inside Bedrock and needs
 *     no brand of its own, and "Bedrock AI" collides head-on with Amazon
 *     Bedrock, which is an actual AI platform. That is the one combination to
 *     avoid on an AI feature.
 *
 * Anything a user reads should come from here rather than from a literal.
 */

export const APP_NAME = "Bedrock";

export const ASSISTANT_NAME = "Assistant";

/** Sentence-case for mid-sentence use: "the assistant is offline". */
export const ASSISTANT_NAME_LOWER = "assistant";
